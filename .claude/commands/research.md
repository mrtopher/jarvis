# /research

Research a question, topic, or set of URLs with grounded multi-source research, capture it as a source-of-truth note in the vault, and optionally hand it off as a formatted Google Doc.

Read and execute the workflow file at `Machine/Workflows/research.md` — follow every step exactly as documented.

The user's arguments (if any) are: $ARGUMENTS
